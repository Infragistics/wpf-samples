<Window x:Class="IGDataChart.Controls.AnnotationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IGDataChart.Controls"
             xmlns:ig="http://schemas.infragistics.com/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center">Label:</TextBlock>
                <TextBox Grid.Column="1" Margin="5 5 5 5" HorizontalAlignment="Stretch" x:Name="LabelTextBox"></TextBox>
            </Grid>
            <!--<TextBox x:Name="InputTextBox"
                  Grid.Row="1"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 />-->
            <wv2:WebView2
                Grid.Row="1"
                x:Name="webView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBlock Margin="2 5 8 5" VerticalAlignment="Center" Grid.Column="0">Badge Color:</TextBlock>
                <ig:XamColorEditor Margin="5" Density="Compact" Grid.Column="1" x:Name="badgeColor" Value="White">

                </ig:XamColorEditor>
                <TextBlock Margin="2 5 8 5" VerticalAlignment="Center" Grid.Column="2">Main Color:</TextBlock>
                <ig:XamColorEditor Margin="5" Density="Compact" Grid.Column="3" x:Name="mainColor" Value="Black">

                </ig:XamColorEditor>
            </Grid>
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1" Margin="0,10,0,0">
            <Button Content="Done" Width="75" Margin="0,0,10,0" Click="Done_Click"/>
            <Button Content="Cancel" Width="75" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
