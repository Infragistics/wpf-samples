<Window x:Class="Infragistics.Framework.Browser.SamplesNavigator"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        mc:Ignorable="d"
        Title="Infragistics Samples Browser" Height="800" Width="1100" >
    <Grid>

        <Grid.Resources>
            <SolidColorBrush x:Key="AppAccentBrush" Color="#FF0078D7"/>
            <SolidColorBrush x:Key="AppBackground" Color="White"/>
        </Grid.Resources>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
  
        <Grid Background="{StaticResource AppAccentBrush}" HorizontalAlignment="Stretch">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text=" SAMPLE: " VerticalAlignment="Center"   Margin="4"  
                  Foreground="{StaticResource AppBackground}"/>
                <ComboBox x:Name="SamplesSelector" 
                  DataContext="{Binding}"  Margin="2"
                  ItemsSource="{Binding Samples}" 
                  SelectedIndex="{Binding SelectedSample, Mode=TwoWay}" 
                  DisplayMemberPath="Name"
                  SelectionChanged="OnSamplesSelectorChanged"  
                  Width="300" HorizontalAlignment="Left" />
            </StackPanel>
        </Grid>
        
        <Grid x:Name="SampleContainer" Grid.Row="1"  Background="Transparent"
              Grid.Column="0"  Margin="0"/>
    </Grid>
</Window>
