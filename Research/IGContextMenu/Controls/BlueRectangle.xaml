<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:IGContextMenu.Controls">
    
    <Style TargetType="local:BlueRectangle">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:BlueRectangle">
                    <Grid>
                        <Rectangle Width="300" Height="200" RadiusX="2" RadiusY="2" Fill="#FF0099FF"/>
                        <TextBlock Text="{TemplateBinding Text}" Foreground="{TemplateBinding TextForeground}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>